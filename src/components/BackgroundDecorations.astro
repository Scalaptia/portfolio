---

---

<div class="fixed inset-0 pointer-events-none overflow-hidden z-[-1] w-full">
  <!-- Decoraciones de fondo con efecto parallax -->
  <!-- Primer grupo - movimiento ultra lento -->
  <div class="absolute w-full parallax-ultra-slow" style="height: 200vh;">
    <div
      class="absolute w-12 h-12 border-4 border-primary/5 rotate-45"
      style="top: 5vh; left: 5%;"
    >
    </div>
    <div
      class="absolute w-[72px] h-[72px] border-4 border-primary/5 -rotate-6"
      style="top: 45vh; left: 70%;"
    >
    </div>
    <div
      class="absolute w-16 h-16 border-4 border-secondary/8 -rotate-12"
      style="top: 65vh; right: 40%;"
    >
    </div>
    <div
      class="absolute w-10 h-10 border-4 border-text/6 rotate-30"
      style="top: 85vh; left: 15%;"
    >
    </div>
    <div
      class="absolute w-[52px] h-[52px] border-4 border-primary/5 -rotate-45"
      style="top: 110vh; right: 8%;"
    >
    </div>
    <div
      class="absolute w-9 h-9 border-4 border-secondary/8 rotate-60"
      style="top: 130vh; left: 60%;"
    >
    </div>
    <div
      class="absolute w-11 h-11 border-4 border-text/6 rotate-15"
      style="top: 150vh; right: 25%;"
    >
    </div>
    <div
      class="absolute w-12 h-12 border-4 border-primary/5 -rotate-30"
      style="top: 170vh; left: 35%;"
    >
    </div>
    <div
      class="absolute w-10 h-10 border-4 border-secondary/8 rotate-75"
      style="top: 190vh; right: 15%;"
    >
    </div>
  </div>

  <!-- Segundo grupo - movimiento lento -->
  <div class="absolute w-full parallax-slow" style="height: 200vh;">
    <div
      class="absolute w-8 h-8 border-4 border-primary/10 rotate-45"
      style="top: 10vh; left: 8%;"
    >
    </div>
    <div
      class="absolute w-10 h-10 border-4 border-secondary/15 -rotate-12"
      style="top: 30vh; right: 12%;"
    >
    </div>
    <div
      class="absolute w-6 h-6 border-4 border-text/8 rotate-12"
      style="top: 50vh; left: 25%;"
    >
    </div>
    <div
      class="absolute w-7 h-7 border-4 border-primary/12 -rotate-45"
      style="top: 70vh; right: 35%;"
    >
    </div>
    <div
      class="absolute w-9 h-9 border-4 border-secondary/15 rotate-30"
      style="top: 90vh; left: 50%;"
    >
    </div>
    <div
      class="absolute w-8 h-8 border-4 border-text/8 -rotate-60"
      style="top: 115vh; right: 5%;"
    >
    </div>
    <div
      class="absolute w-7 h-7 border-4 border-primary/10 rotate-45"
      style="top: 135vh; left: 75%;"
    >
    </div>
    <div
      class="absolute w-10 h-10 border-4 border-secondary/15 -rotate-15"
      style="top: 155vh; right: 50%;"
    >
    </div>
    <div
      class="absolute w-6 h-6 border-4 border-text/8 rotate-90"
      style="top: 175vh; left: 20%;"
    >
    </div>
    <div
      class="absolute w-9 h-9 border-4 border-primary/12 -rotate-30"
      style="top: 195vh; right: 30%;"
    >
    </div>
  </div>

  <!-- Tercer grupo - movimiento medio -->
  <div class="absolute w-full parallax-medium" style="height: 200vh;">
    <div
      class="absolute w-7 h-7 border-4 border-secondary/20 rotate-30"
      style="top: 15vh; left: 80%;"
    >
    </div>
    <div
      class="absolute w-9 h-9 border-4 border-primary/15 -rotate-6"
      style="top: 35vh; left: 3%;"
    >
    </div>
    <div
      class="absolute w-8 h-8 border-4 border-text/10 rotate-60"
      style="top: 55vh; right: 15%;"
    >
    </div>
    <div
      class="absolute w-6 h-6 border-4 border-secondary/18 -rotate-30"
      style="top: 75vh; left: 40%;"
    >
    </div>
    <div
      class="absolute w-7 h-7 border-4 border-primary/15 rotate-45"
      style="top: 95vh; right: 20%;"
    >
    </div>
    <div
      class="absolute w-8 h-8 border-4 border-text/10 -rotate-45"
      style="top: 120vh; left: 65%;"
    >
    </div>
    <div
      class="absolute w-9 h-9 border-4 border-secondary/20 rotate-15"
      style="top: 140vh; right: 45%;"
    >
    </div>
    <div
      class="absolute w-6 h-6 border-4 border-primary/15 -rotate-75"
      style="top: 160vh; left: 10%;"
    >
    </div>
    <div
      class="absolute w-7 h-7 border-4 border-text/10 rotate-30"
      style="top: 180vh; right: 60%;"
    >
    </div>
  </div>

  <!-- Cuarto grupo - movimiento rápido -->
  <div class="absolute w-full parallax-fast" style="height: 200vh;">
    <div
      class="absolute w-5 h-5 border-4 border-primary/25 rotate-45"
      style="top: 20vh; left: 60%;"
    >
    </div>
    <div
      class="absolute w-6 h-6 border-4 border-secondary/20 -rotate-12"
      style="top: 40vh; right: 20%;"
    >
    </div>
    <div
      class="absolute w-4 h-4 border-4 border-text/18 rotate-90"
      style="top: 60vh; left: 20%;"
    >
    </div>
    <div
      class="absolute w-5 h-5 border-4 border-primary/20 -rotate-60"
      style="top: 80vh; left: 10%;"
    >
    </div>
    <div
      class="absolute w-6 h-6 border-4 border-secondary/25 rotate-30"
      style="top: 100vh; right: 35%;"
    >
    </div>
    <div
      class="absolute w-4 h-4 border-4 border-text/18 -rotate-45"
      style="top: 125vh; left: 45%;"
    >
    </div>
    <div
      class="absolute w-5 h-5 border-4 border-primary/25 rotate-60"
      style="top: 145vh; right: 10%;"
    >
    </div>
    <div
      class="absolute w-6 h-6 border-4 border-secondary/20 -rotate-30"
      style="top: 165vh; left: 70%;"
    >
    </div>
    <div
      class="absolute w-4 h-4 border-4 border-text/18 rotate-75"
      style="top: 185vh; right: 25%;"
    >
    </div>
  </div>
</div>

<style>
  /* Solo animaciones sutiles que no interfieran con el parallax */
  .parallax-ultra-slow {
    animation: gentle-pulse 8s ease-in-out infinite;
  }

  .parallax-slow {
    animation: gentle-pulse 6s ease-in-out infinite;
  }

  .parallax-medium {
    animation: gentle-pulse 4s ease-in-out infinite;
  }

  .parallax-fast {
    animation: gentle-pulse 3s ease-in-out infinite;
  }
  @keyframes gentle-pulse {
    0%,
    100% {
      opacity: 0.3;
    }
    50% {
      opacity: 0.7;
    }
  }

  /* Rotaciones personalizadas */
  .rotate-15 {
    transform: rotate(15deg);
  }
  .rotate-30 {
    transform: rotate(30deg);
  }
  .rotate-60 {
    transform: rotate(60deg);
  }
  .rotate-75 {
    transform: rotate(75deg);
  }
  .-rotate-60 {
    transform: rotate(-60deg);
  }
  .-rotate-75 {
    transform: rotate(-75deg);
  }
  .-rotate-15 {
    transform: rotate(-15deg);
  }

  /* Preparación para parallax */
  @media (prefers-reduced-motion: no-preference) {
    .parallax-ultra-slow,
    .parallax-slow,
    .parallax-medium,
    .parallax-fast {
      transform: translateZ(0);
      will-change: transform;
    }
  }
  /* Responsivo - menos elementos en móvil */
  @media (max-width: 768px) {
    .parallax-ultra-slow > div:nth-child(n + 5) {
      display: none;
    }

    .parallax-slow > div:nth-child(n + 5) {
      display: none;
    }

    .parallax-medium > div:nth-child(n + 5) {
      display: none;
    }

    .parallax-fast > div:nth-child(n + 5) {
      display: none;
    }

    /* Reduce el tamaño de las decoraciones en tablets */
    .parallax-ultra-slow > div,
    .parallax-slow > div,
    .parallax-medium > div,
    .parallax-fast > div {
      transform: scale(0.8);
    }
  }

  @media (max-width: 480px) {
    .parallax-ultra-slow > div:nth-child(n + 3) {
      display: none;
    }

    .parallax-slow > div:nth-child(n + 3) {
      display: none;
    }

    .parallax-medium > div:nth-child(n + 3) {
      display: none;
    }

    .parallax-fast > div:nth-child(n + 3) {
      display: none;
    }

    /* Reduce aún más el tamaño en móviles */
    .parallax-ultra-slow > div,
    .parallax-slow > div,
    .parallax-medium > div,
    .parallax-fast > div {
      transform: scale(0.6);
    }
  }
</style>

<script>
  // Efecto parallax real basado en scroll
  document.addEventListener("DOMContentLoaded", () => {
    if (window.matchMedia("(prefers-reduced-motion: no-preference)").matches) {
      const parallaxElements = {
        ultraSlow: document.querySelectorAll(".parallax-ultra-slow"),
        slow: document.querySelectorAll(".parallax-slow"),
        medium: document.querySelectorAll(".parallax-medium"),
        fast: document.querySelectorAll(".parallax-fast"),
      };

      let ticking = false;
      function updateParallax() {
        const scrolled = window.pageYOffset;

        // Diferentes velocidades para cada grupo de elementos
        parallaxElements.ultraSlow.forEach((element) => {
          const yPos = -(scrolled * 0.1); // Muy lento
          const currentTransform = (
            element as HTMLElement
          ).style.transform.replace(/translateY\([^)]*\)/g, "");
          (element as HTMLElement).style.transform =
            `${currentTransform} translateY(${yPos}px)`.trim();
        });

        parallaxElements.slow.forEach((element) => {
          const yPos = -(scrolled * 0.2); // Lento
          const currentTransform = (
            element as HTMLElement
          ).style.transform.replace(/translateY\([^)]*\)/g, "");
          (element as HTMLElement).style.transform =
            `${currentTransform} translateY(${yPos}px)`.trim();
        });

        parallaxElements.medium.forEach((element) => {
          const yPos = -(scrolled * 0.3); // Medio
          const currentTransform = (
            element as HTMLElement
          ).style.transform.replace(/translateY\([^)]*\)/g, "");
          (element as HTMLElement).style.transform =
            `${currentTransform} translateY(${yPos}px)`.trim();
        });

        parallaxElements.fast.forEach((element) => {
          const yPos = -(scrolled * 0.4); // Rápido
          const currentTransform = (
            element as HTMLElement
          ).style.transform.replace(/translateY\([^)]*\)/g, "");
          (element as HTMLElement).style.transform =
            `${currentTransform} translateY(${yPos}px)`.trim();
        });

        ticking = false;
      }

      function requestTick() {
        if (!ticking) {
          requestAnimationFrame(updateParallax);
          ticking = true;
        }
      }

      window.addEventListener("scroll", requestTick, { passive: true });
    }
  });
</script>
